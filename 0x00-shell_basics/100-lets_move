#!/bin/bash

# Create the /tmp/u directory if it doesn't exist
mkdir -p /tmp/u

# Find all files beginning with an uppercase letter in the current directory
files=$(find . -maxdepth 1 -type f -name '[A-Z]*')

# Move the files to /tmp/u
mv $files /tmp/u
